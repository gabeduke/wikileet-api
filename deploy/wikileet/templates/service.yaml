apiVersion: v1
kind: Service
metadata:
  name: {{ include "wikileet.fullname" . }}
  labels:
    {{- include "wikileet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wikileet.selectorLabels" . | nindent 4 }}