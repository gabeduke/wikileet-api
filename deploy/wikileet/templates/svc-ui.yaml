apiVersion: v1
kind: Service
metadata:
  name: {{ include "wikileet-ui.name" . }}
  labels:
    {{- include "wikileet-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wikileet-ui.selectorLabels" . | nindent 4 }}